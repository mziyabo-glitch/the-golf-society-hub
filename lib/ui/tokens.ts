/**
 * Design tokens — re-export hub for spacing, radii, type scale, elevation, and motion-friendly sizes.
 * Prefer importing from here or `@/lib/ui/theme` in new code; both stay in sync.
 */

export {
  borderWidth,
  buttonHeights,
  cardElevation,
  colors,
  iconSize,
  premiumTokens,
  radius,
  shadows,
  spacing,
  typography,
} from "./theme";

export type { ThemePaletteMode, ThemePreference, TypographyVariant } from "./theme";
