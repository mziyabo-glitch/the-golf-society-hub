# Supabase Auth Load Test — Environment Variables
# Copy to .env and fill in your values.

SUPABASE_URL=
SUPABASE_ANON_KEY=

# Password used for all test accounts
TEST_PASSWORD=Password123!

# Number of users to create / log in (override via CLI arg)
N=20

# Unique run identifier (auto-generated if blank)
RUN_ID=

# Max concurrent requests per batch (keep low to reduce rate limiting)
BATCH_SIZE=10

# Delay between batches in ms
BATCH_DELAY_MS=1000
