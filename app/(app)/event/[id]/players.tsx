/**
 * Event Players Screen — STUB for React #310 isolation.
 * Minimal component: no hooks, no Screen wrapper.
 * If #310 still happens here, the bug is in an ancestor/layout/guard.
 */

import { View, Text } from "react-native";

export default function EventPlayersScreen() {
  console.log("PLAYERS_STUB_RENDER");
  return (
    <View style={{ padding: 24 }}>
      <Text>PLAYERS_STUB_RENDER</Text>
    </View>
  );
}
